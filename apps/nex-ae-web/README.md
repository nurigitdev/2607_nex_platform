# nex-ae-web

Slice 0001 shell for the Korean-default NeX Agent Experience workspace.

Run locally:

```bash
npm --prefix apps/nex-ae-web run dev
```

The shell uses only Node.js standard library for serving static files.
